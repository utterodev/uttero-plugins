---
name: access
description: Manage the Uttero caller allowlist — control who can push voice events into your session. Use when the user wants to allow or block callers.
---

# Uttero Access Control

Manage the caller allowlist at `~/.uttero/access.json`.

**Note:** Caller access control is planned for a future release. Currently all authenticated callers can reach your session.
